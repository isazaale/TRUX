{
  "name": "trux",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --parallel --filter web dev --filter api start",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.10.10"
  },
  "packageManager": "pnpm@10.5.2"
}
